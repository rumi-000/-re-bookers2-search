<h2>Results index</h2>

<table class="table table-hover table-inverse">
  <% @range == "Book" %>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.body %></td>
        </tr>
      <% end %>
    </tbody>
  <% end %>
</table>